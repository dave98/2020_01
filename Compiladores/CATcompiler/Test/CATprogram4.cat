principal()->retorna id:
  variable s1, s2, s3, s4 = 12;
  variable eye, nose, hand = 10 + 102;
  para variable eye2 = 10 considerando ( eye2 <= s1 ) entonces:
    eye2 += 1;
  fin ;

  $ Comentrio aleatorio $
  si s1 < s2 entonces :
    s1  = s2;
  sino s1 > s2 entonces:
    s1 = s3;
  finalmente :
    s4 = 0 ;
  fin;
